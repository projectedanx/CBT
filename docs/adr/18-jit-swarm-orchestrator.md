# Architecture Decision Record: SCOS JIT Swarm Orchestrator

## Date
2026-09-19

## Status
Accepted

## Context
Standard execution models couple high-entropy reasoning directly to low-entropy syntactic formatting. This forces a 10% to 30% "Projection Tax" on reasoning capabilities. Furthermore, embedding passive tool definitions inside the context window persistently consumes up to 50% of context, leading to "Context Rot". A decoupled orchestration model is necessary to enforce strict metrology while maintaining high cognitive capacity and eliminating sycophantic test relaxation.

## Decision
We integrate the **Sovereign Cognitive Operating System (SCOS v6.0-STRICT) JIT Swarm Orchestrator**. The orchestrator decouples the cognitive workload into distinct Verifiable Cognition Stack (VCS) layers:

1.  **Hollow-Core Semantic Planning (Manifold $\alpha$):** High-level strategic reasoning is executed freely, detached from tooling schemas.
2.  **Ephemeral JIT Micro-Agents (Manifold $\beta$):** Specialized sub-agents spawn dynamically to run zero-entropy syntactic realizations, returning highly compressed structured summaries to prevent context rot.
3.  **Draft-Conditioned Constrained Decoding (DCCD):** Eliminates projection tax by mapping unconstrained semantic drafts onto rigid Abstract Syntax Tree (AST) schemas via grammar rules.
4.  **Verification Co-Processing & Cache Augmentation:** Integrates with the VCP. When the Confidence-Fidelity Divergence Index (CFDI) breaches the Algorithmic Shame Threshold ($\ge 0.15$), differentiable soft-tokens correct latent trajectories.
5.  **Failure Metabolism (F-IPI):** A strict 3-attempt Loop Constraint enforces the self-correction cycle. Exhausted loops halt execution via Epistemic Escrow, logging Symbolic Scars into the Scar Tissue Archive (STA) to proactively repulse future generations.

## Consequences
- **Positive:** Decoupled architecture eliminates Projection Tax and Context Rot.
- **Positive:** Enforces strict verifiable metrology (CFDI thresholds) preventing logic explosion.
- **Negative/Risk:** The strict Escrow threshold may increase the rate of halted generations and Justified Uncertainty Reports (JURs), necessitating higher operational intervention during untuned executions.
- **Compliance:** Full adherence to AXIOM v1.0 and VULCAN Mereological Mandate.
